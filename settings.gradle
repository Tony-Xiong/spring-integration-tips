pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            url 'https://maven.aliyun.com/repository/gradle-plugin'
        }
    }
}

rootProject.name = 'spring-integration-tips'

file("${rootDir}/apps").eachDir {
    include "apps:${it.name}"
}